import React from 'react';

export const metadata = {
    title: 'YouTube サムネURL取得 | YouTube Link Service'
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
