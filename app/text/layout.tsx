import React from 'react';

export const metadata = {
    title: 'YouTube Markdown Link | YouTube Link Service'
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
