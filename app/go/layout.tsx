import React from 'react';

export const metadata = {
    title: 'リダイレクト | YouTube Link Service'
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
